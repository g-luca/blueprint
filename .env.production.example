# Copy to .env.production (gitignored) and fill in your Worker URL.
# This is read by Vite at build time — must be set before running `bun run build`.
VITE_COLLAB_ENDPOINT=https://blueprint.your-account.workers.dev
